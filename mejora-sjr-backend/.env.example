# Copia este archivo a ".env" y completa los valores reales.
# IMPORTANTE: el archivo ".env" nunca debe subirse a GitHub (agrégalo a .gitignore).

# --- Azure SQL ---
DB_SERVER=servidor-mejorasjr-isaac.database.windows.net
DB_DATABASE=MejoraSJR_DB
DB_USER=admin_mejorasjr
DB_PASSWORD=

# --- Servidor Express ---
PORT=3000

# --- Firebase (existente) ---
# FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json
